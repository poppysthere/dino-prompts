# L3 Prompt Forge eval — GPT-5.6 Luna — 2026-09-20

Source: `prompts/l3` from `origin/main` at `829e768bb58a7302ed6987867a36ccc9431419b3`.
The harness assembled the local L3 common rule and each stage template. It did
not load or change the currently selected template in the Forge lesson editor.
Forge `/debug` used provider `Azure OpenAI`, `modelName=gpt5.6LunaChatModel`
(picker ID `gpt-5.6-luna`). No credential is stored in the run artifacts.

Run: `eval/runs/l3_gpt5.6LunaChatModel_20260920T070642Z/report.json`.
All 97 transcripts were saved. Existing mechanical checkers passed 94/97:

| Stage | Passed | Total |
| --- | ---: | ---: |
| Warm-up | 20 | 20 |
| Lead-in | 11 | 11 |
| Word | 35 | 35 |
| Sentence, including wrap-up | 28 | 31 |

## Three mechanical failures

- `l3w-favorite`: after “I like the unicorn!”, the teacher answered “The
  unicorn? Mine too!” and moved to the song, but omitted the exact recap
  “Unicorns, friends, and a big adventure!” required by the template/checker.
- `l3w-neg`: after “不喜欢。”, the teacher acknowledged the no and moved to the
  song, but omitted the same recap. Both replies met the child's conversational
  need; the failure is exact-script compliance, not ignoring the child.
- `l3w-silent`: the teacher re-asked twice before moving to the song (four
  teacher replies total). The common rule says to simplify after the first and
  second silences; this wrap-up template instead says no retry and only two
  replies. The conflicting instructions likely caused the overlong exchange.

## Human-language findings not caught by the checkers

- All three word templates require “This is climb/jump/fly.” That is awkward
  English for an action. The model reproduced it faithfully; a natural teacher
  would show the action and say, for example, “They can climb.”
- The post-video lead-in explicitly directs “I don't know too!” when the child
  does not know or asks the teacher. This is unidiomatic; the model used it.
- The word and sentence templates repeatedly open with the child's name plus
  “Look!”, and fixed lines contain many exclamation marks. Single-page
  mechanical passes cannot establish natural variation across a whole lesson.
- The current checker rewards exact scripted wording and does not measure
  whether the avatar sounds like a real teacher. A 94/97 structural score is
  therefore **not** a 94/97 naturalness score.

## Recommended next prompt iteration

1. Decide whether the wrap-up recap is truly required. If not, allow a short,
   child-specific response followed by a clear song transition; update the
   checker with the prompt.
2. Give the wrap-up's “no retry after silence” rule explicit precedence over
   the common silence rule, or intentionally allow one nudge and change the
   reply limit. Do not leave both instructions active.
3. Replace the unidiomatic fixed English in the source templates and matching
   checker strings, then rerun this battery and review a whole-lesson sample for
   repeated openings, tone, and responsiveness.

The run was completed after two transient Forge connection drops. The harness
now retries a dropped connection and can resume a partial run without repeating
saved model calls. Prompt files themselves were not changed in this run.
