# Case battery: 新手引导体验课 trial demo — wrap-up (recap + LAST shadow + finale video).
# Only 2 steps exist on device: pre-video (teacher) and video (the giraffe
# reveal, which ENDS the class). So pre-video carries everything: recap cheer
# (hedgehog + flamingo), one guess round about the last shadow — NO goodbye, the
# avatar watches the finale WITH the child. 2 replies, the second launching the final video with [NEXT_STEP].
# Run: python3 eval/run_wrap_trial.py

pre_video:
  - id: wrap-happy
    note: "child guesses a cat — recast + wonder, then the watch launch; giraffe stays secret"
    turns:
      - "A cat!"
    require_phrases: ["hedgehog", "flamingo", "cat\\b.{0,30}maybe", "watch and see"]
    forbid_phrases: ["giraffe"]
  - id: wrap-secret-guess
    note: "child guesses the secret ('长颈鹿！') — recast in English + Maybe, never confirmed; video does the reveal"
    turns:
      - "长颈鹿！"
    require_phrases: ["giraffe\\b.{0,30}maybe", "watch and see"]
    forbid_phrases: ["(yes|yeah|right)[^.!?]*giraffe", "it('s| is) (a |the )?giraffe"]
  - id: wrap-idk
    note: "'I don't know' is an answer, owned together — then the launch; the video IS the answer"
    turns:
      - "不知道。"
    require_phrases: ["me too|i do(n't| not) know|no idea", "watch and see"]
    forbid_phrases: ["giraffe"]
  - id: wrap-question
    note: "child asks who it is back — the true tiny answer ('I don't know yet!'), secret safe, then the launch"
    turns:
      - "是谁呀？"
    require_phrases: ["do(n't| not) know", "watch and see"]
    forbid_phrases: ["giraffe"]
  - id: wrap-silent
    note: "silent child — recap + ask still happen, then no catch, straight to the launch"
    turns: []
    require_phrases: ["hedgehog", "flamingo", "watch and see"]
    forbid_phrases: ["giraffe"]
  - id: wrap-junk-name
    note: "junk booking name is never spoken at the launch either"
    student_name: test_user
    turns:
      - "A dog!"
    require_phrases: ["watch and see"]
    forbid_phrases: ["test_user", "giraffe"]
